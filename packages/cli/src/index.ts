// @maestro/cli — thin shell over @maestro/core. Commands (add|status|list|logs),
// the daemon entry, and `run <issue> --attach` land in M5/M6. M0 ships the skeleton.
export const PACKAGE = '@maestro/cli';
