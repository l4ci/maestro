// Bootstrap marker (AM-13). Placed on the "define my workflow" onboarding issue
// body (§16) so a repeated `maestro add` is idempotent (greppable). M8 consumes it.

export const BOOTSTRAP_MARKER = '<!-- maestro:bootstrap -->';
